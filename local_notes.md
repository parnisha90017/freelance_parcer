# Local Notes

## Что запомнить на завтра
- День 4 закрыт: все 7 парсеров уже работают на VPS.
- Telegram channels parser ещё требует финального селектора.
- Основной следующий шаг — добить TG-каналы и продуктовые улучшения.
- Бот уже работает через inline-кнопки и FSM.
- Для отладки использовать `python test_env/test_bot.py`, а не основной запуск.

## Актуальный статус
- Kwork, FL.ru, Freelance.ru, Weblancer, YouDo, Pchel.net и Freelancehunt — рабочие.
- YouDo парсится через JSON API, без браузера.
- Telegram-каналы — в процессе, парсинг уже виден, нужен окончательный селектор.

## Инструкция для себя
- Сначала обновляй markdown-заметки, потом код.
- Любые новые фичи проверяй в `test_env/` перед основным ботом.
- Если меняется логика уведомлений, фиксируй это в `PROJECT_LOG.md` и `agent_docs/`.

## Важные детали
- Keywords лежат в `data/keywords.json`.
- Бот запускается через `python bot/main_bot.py`.
- Парсер удобно тестировать через `python test.py`.
- Уведомления отправляются с кнопкой на заказ.

## Идеи для улучшения
- Подключить PostgreSQL для истории проектов.
- Добавить защиту от повторных уведомлений.
- Вынести логирование в `loguru`.
- Сделать автоматический запуск по расписанию.
